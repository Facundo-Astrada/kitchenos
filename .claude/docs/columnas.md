# Columnas no intuitivas — verificar antes de queries

| Tabla | Columna correcta | NO usar |
|---|---|---|
| `productos` | `stock_actual`, `stock_minimo`, `stock_critico` — **`stock_minimo` y `stock_critico` son NOT NULL** (DEFAULT 0). Al resetear umbrales usar `update({ stock_minimo: 0, stock_critico: 0 })`, nunca `null`. | `cantidad` |
| `productos` | `precio_unitario` | `precio` |
| `productos` | `es_produccion BOOLEAN DEFAULT false` + `receta_id UUID NULL` (FK recetas, ON DELETE SET NULL) — producción interna (caldo/masa/fondo): el costo se toma de la receta vinculada (`food_cost.costo_porcion`), no de factura. Badge "Producción" en stock. | (agregado jun 2026) |
| `productos` | `sector_id UUID NULL` (FK `stock_sectores`, ON DELETE SET NULL) — sector físico (almacén, cámara, heladera, freezer, producción, cava) donde vive el producto. Un producto pertenece a UN sector (v1). Se asigna desde el modal de edición o en batch desde el sheet de Stockear ("Asignar sector"). `fuera_de_uso BOOLEAN NOT NULL DEFAULT false` — sigue contando en el valor del stock (es capital) pero `calcEstado` (`useStock.ts`) lo fuerza a `'ok'` (nunca genera alerta crítico/bajo/pendiente), queda excluido de `sugerir-minimos`, de la sugerencia de carrito y del recorrido de Stockear; se ve atenuado (`opacity: .55`) con badge gris "Fuera de uso" en la tabla. | (agregado jul 2026 — F3/F4) |
| `stock_sectores` | tabla nueva (jul 2026): `id, restaurante_id, nombre, icono, orden, created_at, ultimo_conteo_at` — sectores físicos de stock, creados inline desde el sheet de Stockear (`ClientView.tsx`). `icono` es un Material Symbol de un preset fijo (`shelves`, `ac_unit`, `kitchen`, `severe_cold`, `skillet`, `wine_bar`). `ultimo_conteo_at TIMESTAMPTZ NULL` — se pisa con `now()` al terminar (llegar a `isLast`) un recorrido de Stockear scopeado a ESE sector (no se actualiza con "Todo el stock" ni por categoría, ni si se sale a mitad de camino). `NULL` = nunca contado. Sumada a `reset_demo_restaurante()` (clona antes que `productos`, que la referencia). | — |
| `stock_estantes` | tabla nueva (jul 2026, board de Stock en Mesa de trabajo): `id, restaurante_id, sector_id (FK stock_sectores, ON DELETE CASCADE), nombre, orden, created_at` — sub-niveles dentro de un sector físico (ej. Cámara → Estante 1/2/3). `ON DELETE CASCADE` en `sector_id`: borrar un sector borra sus estantes en cascada. Sumada a `reset_demo_restaurante()` (clona después de `stock_sectores`, antes de `productos`). | — |
| `productos` | `estante_id UUID NULL` (FK `stock_estantes`, ON DELETE SET NULL) — `NULL` = producto suelto en el sector sin estante asignado (válido, no todo sector necesita sub-niveles). `orden_sector INT NOT NULL DEFAULT 0` — posición manual dentro del estante (o del sector si no tiene estante), fijada arrastrando en el board de Stock (Mesa de trabajo). Ese orden alimenta el recorrido de Stockear cuando se stockea un sector con layout definido. | (agregado jul 2026) |
| `tareas` | `status` (`'pendiente'|'en_proceso'|'completada'`) | `completada` (bool) |
| `tareas` | `fecha_limite` | `fecha_vencimiento` |
| `recetas` | `activa` (bool, soft-delete), `status` (`'published'|'draft'`) | `deleted`, `activo` |
| `recetas` | `tiempo_min` (int) | `tiempo_minutos` |
| `ingredientes` | `producto_id` (FK), `costo_unitario`, `unidad_costo`. **jul 2026 (Tanda D):** columnas OPS por ingrediente (mismo set que `menu_preparaciones`) — `plaza, seccion_mise, cantidad_ops, unidad_ops, recipiente_nombre, peso_porcion, peso_porcion_unidad`. Se usan cuando la receta es `es_plato`: cada ingrediente/subreceta se rutea a una plaza con el `OpsPanel`; las subrecetas (tienen `subreceta_id`) alimentan el mise vía `upsertMiseChecklistItem`. | (sin FK = no link) |
| `ingredientes` | `grupo TEXT NULL` (jul 2026, Sesión 1 B1) — etapa/sección editable dentro de la receta (ej. "Etapa 1 — marinada"). `NULL` = sin etapa, se agrupa bajo "General" en la ficha (`recetario/[id]`) y en el editor de creación (`recetario/page.tsx`). El food cost sigue calculándose sobre la lista plana — `grupo` solo agrupa visualmente. | — |
| `recetas` | `es_plato BOOLEAN DEFAULT false` (jul 2026, Tanda D) — modo "trabajar como plato" (flag **explícito**, elegido al cargar): la ficha muestra botón OPS por ingrediente. **NO confundir** con el derivado `en_carta` de `useRecetas` (existe un `carta_item` con `receta_id`), que antes se llamaba `es_plato` — se renombró para no colisionar con esta columna real. | — |
| `turnos` | UNIQUE (`miembro_id`, `fecha`) — hacer upsert | insert directo |
| `facturas` | `condicion_pago` (`'contado'|'cuenta_corriente'|'30dias'|'60dias'`), `status` (`'pagada'|'pendiente'|'confirmada'|'observada'`). **"Por pagar"** = condicion_pago a crédito (cuenta_corriente/30dias/60dias) ∧ status≠pagada (`esPorPagar` en `facturas/page.tsx`). `pedido_id UUID NULL` (FK pedidos, ON DELETE SET NULL, jun 2026) — reconciliación factura↔pedido. | — |
| `factura_items` | `producto_nombre` (text, no FK), `precio_unitario` por unidad | (sin link directo a productos) |
| `pedidos` | `status` (`'borrador'\|'enviado'\|'parcial'\|'recibido'`), `fecha_pedido`, `total_estimado`. **Rango de entrega (jun 2026):** `entrega_desde DATE` + `entrega_hasta DATE` — se setean al enviar (`usePedidos.enviarPedido`); la vieja `fecha_entrega_esperada` queda como fallback. `IngresosBanner` usa la ventana [desde,hasta] para avisar qué pedidos llegan hoy / están atrasados. | `fecha_entrega_esperada` sola (deprecada por el rango) |
| `pedido_items` | `producto_nombre`, `producto_id` (FK NULL), `cantidad`, `unidad`, `precio_estimado`, `recibido BOOL`, `cantidad_recibida`. Al recibir (`recibirPedido`) suma `cantidad_recibida` a `productos.stock_actual` matcheando por nombre. | — |
| `plato_componentes` | `plaza`, `cantidad_diaria`, `unidad`, `sync_ops BOOLEAN DEFAULT false` — solo sincroniza al checklist si es `true` | (sync_ops agregado mayo 2026) |
| `plato_plazas` | `plato_id` = `receta_id` (no es `plato_compuesto_id`), `ingredientes text[]` | `receta_id` (la columna se llama `plato_id`) |
| `plato_recetas` | `plaza VARCHAR(50) NULL` — estación de producción asignada al vincular receta a plato | (agregado mayo 2026) |
| `plato_recetas` | `cantidad_ops NUMERIC NULL` + `unidad_ops TEXT NULL` — contribución de ESTE plato al mise. `checklist_items.cantidad` = suma de todas las contribuciones con misma `receta_id+plaza`. NO usar como valor absoluto — siempre recalcular la suma. | (agregado junio 2026) |
| `puestos` | `nivel TEXT DEFAULT 'cocinero'` → mapea a `rol_permisos.rol` (admin/sous_chef/cocinero/bachero). `plaza_default TEXT` → plaza OPS por defecto del puesto. `permisos_app TEXT[]` → ahora guarda `ModuloId[]` reales (no texto libre). | (agregado junio 2026) |
| `equipo_miembros` | `modulos_extra TEXT[]` — módulos adicionales habilitados individualmente más allá del puesto. `modulos_restringidos TEXT[]` — módulos removidos individualmente. Combinar con `puestos.permisos_app` para obtener módulos efectivos. `costo_hora NUMERIC NULL` (jul 2026, M3) — solo admin lo ve/edita en el form (gate `isAdmin` en UI, no RLS); se une contra `turnos_personal.usuario_id = equipo_miembros.auth_user_id` para calcular costo laboral en Reportes. | (agregado junio 2026) |
| `turnos_personal` | **SÍ EXISTE** (corrección jul 2026 — la fila de abajo estaba desactualizada; la tabla se creó en algún momento entre junio y julio 2026, con RLS estándar): `id, restaurante_id, usuario_id` (FK `auth.users.id`, NO `equipo_miembros.id`)`, fecha, entrada, salida, horas_total` + `editado_por TEXT NULL, editado_at TIMESTAMPTZ NULL` (M3, jul 2026). **`horas_total` es columna `GENERATED ALWAYS AS (EXTRACT(epoch FROM (salida-entrada))/3600)` — nunca mandarla en INSERT/UPDATE**, Postgres devuelve 400 `"column horas_total can only be updated to DEFAULT"` (código `428C9`). Usada por fichaje real (dashboard Iniciar/Cerrar turno + tab Fichajes en Turnos, `lib/hooks/useFichaje.ts`) y por `reportes/personal` (eficiencia). Múltiples filas por persona/día son válidas (turnos partidos almuerzo/cena) — `fetchFichajeAbierto` toma la más reciente con `salida IS NULL`. | `completada`, mandar `horas_total` a mano |
| `checklist_items` | `seccion_id` (FK a `checklist_secciones`), `seccion` (texto legacy) | usar `seccion_id` para lookups |
| `checklist_secciones` | `tipo TEXT NOT NULL DEFAULT 'produccion'` (jul 2026, Sesión 2 B3) ∈ `'produccion'\|'almacen'\|'heladera'\|'freezer'\|'estacion'` — `'produccion'` es el default, no rompe secciones existentes (mise diario). `producto_ids UUID[] NOT NULL DEFAULT '{}'` — solo se usa cuando `tipo='almacen'`: productos del stock que "Stockear sección" (Mesa de Trabajo) actualiza en batch. Heladera/Freezer no usan `producto_ids`, se linkean a HACCP por **nombre** (`haccp_equipos.nombre ilike checklist_secciones.nombre`, sin FK — ver `HaccpSeccionLink.tsx`). RLS de esta tabla **ya estaba correcta** (`restaurante_id = mi_restaurante_id()` en las 4 policies) — el plan asumía `USING(true)`, desactualizado. | (columna `tipo` sin esto = trata todo como producción) |
| `checklist_secciones` | `parent_id UUID NULL REFERENCES checklist_secciones(id) ON DELETE CASCADE` (jul 2026) — sub-secciones dentro de una sección (Mesa de Trabajo + Mise). `NULL` = sección raíz. **v1: solo 1 nivel de profundidad** — una fila con `parent_id` no puede a su vez ser padre de otra; es una regla de UI (`SectionEditor.tsx`/`OpsPanel.tsx`/`SeccionRow.tsx` con `depth`, ver `seccionTieneContenido` en `lib/checklist/secciones.ts`), no un CHECK en la DB, para poder relajarla después sin migración. Borrar una sección con hijos los borra en cascada (bloqueado en la UI si la sección o algún hijo tiene contenido). Mismo patrón que `tareas.parent_id` (subtareas). | (agregado jul 2026) |
| `checklist_items` | `plaza = 'general'` — aparece en el checklist de TODAS las plazas, no solo la propia | (distinguir de plazas específicas) |
| `checklist_items` | `recipiente_nombre TEXT NULL` — nombre del recipiente (tupper, cubeta GN, etc.). `recipiente_capacidad NUMERIC NULL` — capacidad en porciones (siempre en porc, aunque el usuario haya cargado en g/kg se convierte). `peso_porcion NUMERIC NULL` + `peso_porcion_unidad TEXT NULL` — peso/tamaño de 1 porción (puede ser en g, kg, ml, l, u, porc). Si ambos están en peso, `recipiente_capacidad = Math.round(capG / porG)`. Usados por `ProductoMiseCard` para calcular déficit y CTA "Producir X porc (Yg)". | (agregado jun 2026 — sistema de recipientes en mise) |
| `produccion_diaria` | `menu_tag TEXT NULL` — null = menú base del día, string = nombre de evento/menú específico | (no existía antes de mayo 2026) |
| `carta_items` | `tags TEXT[] DEFAULT '{}'` — dietarios: `'s/tacc'`, `'vegano'`, `'vegetariano'`, `'keto'`, `'picante'`, `'sin lactosa'` | (agregado mayo 2026) |
| `carta_items` | `estacion_default_id UUID NULL` (FK `estaciones`, ON DELETE SET NULL) — estación KDS por defecto del ítem (regla de ruteo: a qué pantalla va al enviarse). Si NULL, el ítem queda sin asignar. Usado por `useComandas.agregarItems` para setear `comanda_items.estacion_id`. | (agregado jul 2026) |
| `carta_categorias` | tabla nueva: `id, nombre, icono, orden, restaurante_id` — categorías dinámicas por restaurante | no usar `CATEGORIAS` hardcodeado en código |
| `haccp_limpieza` | `dia_semana INT` (0=Dom..6=Sáb, para frecuencia semanal), `dia_mes INT` (1-31, mensual), `sync_ops BOOLEAN DEFAULT true`, `checklist_item_id UUID` — con `sync_ops` crea una **`checklist_rutina`** (NO `checklist_items`) en plaza `general`; `checklist_item_id` guarda el id de esa rutina. Aparece SOLO en la pestaña Rutina del Mise, el día que corresponde. Borrar la limpieza borra la rutina. | (sync reescrito jun 2026: antes creaba checklist_items en apertura/cierre) |
| `checklist_rutina` | `dias_semana INT[]` (ISO 1=Lun..7=Dom; null = todos los días), `dia_mes INT` (1-31, para mensual; agregado jun 2026). El render filtra por día: semanal→su día, mensual→su fecha, diaria/sin día→siempre. Lo usan la pestaña Rutina del Mise y `RutinasDia` en Planificación. | (HACCP→Limpieza con sync inserta acá) |
| `presupuestos` | tabla nueva: `id, restaurante_id, periodo, monto, created_at, updated_at` — `periodo` ∈ `'semanal'\|'mensual'\|'trimestral'\|'semestral'\|'anual'`, UNIQUE(restaurante_id, periodo) → upsert | (junio 2026, Reportes Presupuesto vs Real) |
| `restaurantes` | `configuracion JSONB` — guarda `{ nombres_excluidos: string[] }` (empleados/socios a excluir del OCR de facturas) y `onboarding_step` | (no es columna plana) |
| `restaurantes` | `slug TEXT UNIQUE NULL` (carta pública, generado con slugify propio en `configuracion/page.tsx`, editable), `carta_publica_activa BOOLEAN DEFAULT false` — toggle admin. `00000000-…-000001`=El Rescoldo real, `00000000-…-000002`=El Rescoldo Demo (clon de solo-demo, ver `demo_visitas` abajo). | (agregado jul 2026, Q1) |
| `ventas` | `total_ventas`, `cantidad_cubiertos`, `fecha` (date) | — |
| `ventas_items` | `nombre_plato` (no FK), `cantidad`, `precio_unitario`, `subtotal` | — |
| `menus` | tabla nueva (jun 2026): `id, restaurante_id, nombre, tipo ('fijo'\|'evento'), descripcion, activo, created_at, updated_at` — capa por encima del plato (Carta → Menú → Producción). **jul 2026:** `fecha_evento DATE NULL` (solo eventos), `precio NUMERIC NULL` (un precio; las variantes lo comparten), `variantes TEXT[] NULL` = lista de nombres de variante del menú (composiciones alternativas a un precio, el comensal elige una). | — |
| `menu_preparaciones` | tabla nueva (jun 2026): `menu_id` (FK CASCADE), `paso` (curso/Apetizer…), `tipo ('plato'\|'receta'\|'producto'\|null)` + `ref_id UUID` (polimórfico a carta_items/recetas/productos, sin FK dura), `nombre`, `prioridad ('critica'\|'alta'\|'media'\|'baja')`, `plaza`, `seccion_mise` (sección fina del checklist de mise), `usuario_asignado` (TEXT = equipo_miembros.id, igual que produccion_diaria), `cantidad`, `unidad`, `orden`. **jul 2026:** `variante TEXT NULL` = **NULL = ítem común a todas las variantes** (ej. entrada/postre), un texto = ítem exclusivo de esa variante (ej. el principal); mapea a `menus.variantes`. OPS por ítem (mismo panel que plato/ficha): `cantidad_ops NUMERIC`, `unidad_ops TEXT`, `recipiente_nombre TEXT`, `peso_porcion NUMERIC`, `peso_porcion_unidad TEXT`. Al editar un menú viejo, `plaza`/`seccion_mise` pueden tener texto libre; el `OpsPanel` ahora los espera como id de `PLAZAS_OPS`/`SECCIONES_OPS` (los que no matchean no resaltan el chip; los nuevos quedan consistentes). | — |
| `tareas` | `seccion` es **NOT NULL** (default mandar `'general'` si no hay valor). `modo` ('carta' agrupa por prioridad / 'menu' agrupa por sección). `menu_id UUID NULL` (jun 2026) = tarea creada al activar un menú; la vista del menú activo en Planificación filtra por `menu_id + turno_fecha`. `estado` (OpsEstado: pendiente/en_curso/listo/duda) = el tildable en verde. `asignado_a` TEXT (equipo_miembros.id). **Carryover (jun 2026):** la vista de Producción muestra solo `turno_fecha === hoy` + las de **ayer** sin completar (carryover de 1 día) y **requiere `turno_fecha`** (las NULL ya no se muestran — antes se acumulaban para siempre). `activarMenu` deduplica por preparación contra el día y el arrastre de ayer; `actualizarMenu` propaga ediciones del menú a las tareas ya activadas (hoy en adelante). | `completada` (no existe) |

## Unidades de ingredientes — trampas de conversión

`ingredientes.unidad` y `ingredientes.unidad_costo` pueden llegar con variantes no estándar desde importaciones:

| Lo que viene | Lo correcto | Nota |
|---|---|---|
| `gr`, `grs`, `gramo` | `g` | muy frecuente en Bros |
| `lt`, `lts`, `litro` | `l` | |
| `cc`, `mililitro` | `ml` | |
| `unidad`, `unidades`, `un` | `u` | muy frecuente — venía de productos.unidad |

El código en `lib/hooks/useRecetas.ts` canoniza via `canonUnit()` antes de calcular el factor. La migración `supabase/migrations/normalizar_unidades_ingredientes.sql` los corrige en DB.

**Combo imposible `unidad='g/kg/ml/l'` vs `unidad_costo='u'` (o viceversa):** factor = **0** → la línea se excluye del costo en vez de inflarlo. Ej: "4 u de Laurel" a $18.595/kg daba $74.380. Son datos a corregir a mano (setear el peso real por unidad o cambiar la unidad del producto).

**Productos con `unidad='unidad'` y precio alto**: 182 productos en Bros tienen `unidad='unidad'` pero su precio es en realidad por kg/l (ingresados con unidad incorrecta desde facturas). Esto subvalúa 53 recetas. El código los protege con factor 0. Corregir manualmente en Stock editando la unidad del producto.

**Categorías de `productos`**: 16 categorías canónicas: `Carnes`, `Pescados`, `Verduras`, `Frutas`, `Lácteos`, `Panadería`, `Secos`, `Especias`, `Bebidas`, `Aceites`, `Vinagres`, `Conservas`, `Congelados`, `Limpieza`, `Descartables`, `Otros`. Usar siempre estas. Para re-categorizar en bulk: `scripts/recategorizar-productos.mjs --apply` (reglas + Haiku + guard).

| `checklist_items` | `demanda_viva INTEGER DEFAULT 0` — porciones pedidas desde el salón en el turno actual. Incrementado por `POST /api/salon/prep-list-update` cuando se envía una comanda (fire-and-forget). Se reinicia manualmente al aperturar. `ProductoMiseCard` puede usarlo para mostrar cuánto falta producir. | (agregado jul 2026) |
| `recetas` | `foto_url TEXT NULL` — URL pública en bucket `fotos` de Supabase Storage. `peso_total_g NUMERIC NULL` — peso bruto total de la receta (suma de ingredientes o manual). `peso_escurrido_g NUMERIC NULL` — peso neto tras cocción/escurrido. | (agregado jul 2026) |
| `carta_items` | `foto_url TEXT NULL` — ya existía en DB y en `useCarta`. Bucket `fotos`. Se pasa en `crearItem` y `actualizarItem`. | (verificado jul 2026) |
| `carta_items` | `receta_id UUID NULL` = **link 1:1 "la receta ES un plato"** (jul 2026). "Convertir a plato" en `recetario/[id]` crea el `carta_item` con este FK; el costo/FC se leen en vivo de la receta (rama fallback de `useCarta.fetchCartaItemsData`). **`recetas.es_plato` NO es columna** — es derivado en `useRecetas` (query a `carta_items` con `receta_id` no null → `Set`); si se borra el plato, el flag desaparece solo. | — |
| `recetas` | `vida_util_dias INT NULL` (agregado jul 2026, Q4) — días hasta caducidad de la producción. `NULL` → usar 3 días de default al imprimir la etiqueta (fallback en el cliente, no en DB). | — |
| `cajas_turnos` | tabla nueva (jul 2026, M1): `id, restaurante_id, estado ('abierta'\|'cerrada'), abierta_por, fecha_apertura, monto_inicial, cerrada_por, fecha_cierre, montos_esperados JSONB, montos_declarados JSONB, diferencia_total, arqueo_ciego, notas`. `montos_esperados`/`montos_declarados` son objetos `{ [medio_id]: monto }` — **snapshot al momento del cierre**, no se recalculan después aunque cambien los `pagos` históricos. Índice único parcial `WHERE estado='abierta'` — solo puede haber una caja abierta por restaurante a la vez (el INSERT de una segunda apertura falla por constraint, no por lógica de app). `abierta_por`/`cerrada_por` son `TEXT` (equipo_miembros.id), mismo patrón que `tareas.creado_por`. | — |
| `caja_movimientos` | tabla nueva (jul 2026, M1): `id, caja_turno_id, medio_id, tipo ('retiro'\|'ingreso'), monto, motivo, creado_por, created_at`. Retiros/ingresos intermedios durante un turno de caja abierto. `medio_id` es NOT NULL (FK `medios_pago`) — el fondo inicial de un retiro/ingreso de efectivo se elige por heurística de nombre (`inferMedioEfectivo` en `useCajaTurno.ts`, regex `/efectivo\|cash/i` sobre `medios_pago.nombre`) porque la tabla `medios_pago` no tiene un campo "tipo". | — |
| `mesas` | `forma TEXT NOT NULL DEFAULT 'cuadrada'` ∈ `'cuadrada'\|'redonda'\|'rectangular'` + `ancho NUMERIC NOT NULL DEFAULT 8` + `alto NUMERIC NOT NULL DEFAULT 8` (ambos % del canvas, mismo sistema que `pos_x`/`pos_y` ya existentes) + `rotacion INT NOT NULL DEFAULT 0` (el panel ofrece 0/45/90 pero es INT libre) + `color TEXT NULL` (hex elegido por el usuario; NULL = madera `#a9744f` para mesa libre, o color de estado si ocupada) (jul 2026, Sesión 3 C3 + fase salón v2) — usados por el editor visual (`salon/config/page.tsx`) y el mapa real (`salon/page.tsx`, `MesaBoton`) para renderizar la mesa con su forma/tamaño/rotación/color reales. `ancho`/`alto` ahora se editan libremente con el handle de resize (antes 3 tamaños fijos). | (agregado jul 2026) |
| `salon_elementos` | tabla nueva (fase salón v2, jul 2026): `id, restaurante_id, tipo TEXT DEFAULT 'otro'` ∈ `'barra'\|'caja'\|'parrilla'\|'planta'\|'pared'\|'otro'`, `label TEXT NULL`, `pos_x/pos_y/ancho/alto NUMERIC` (% del mundo, mismo sistema que `mesas`), `rotacion INT`, `color TEXT NULL`, `created_at`. Elementos decorativos del plano (mobiliario no-mesa). RLS estándar multi-tenant. Editables en el mismo canvas del editor (`ElementoCanvasItem`), no clickeables en el mapa real (`ElementoDecorativo`, `pointerEvents: 'none'`). Config de tipos (icono/tamaño/color default) en `lib/salon/elementos.ts`. **Ojo:** no está en `reset_demo_restaurante()` — la demo la mostrará vacía tras el próximo reset hasta que se sume a esa función. | (agregado jul 2026) |

## Supabase Storage — bucket `fotos` (jul 2026)

Bucket `fotos` público creado via **SQL directo** (no vía Storage REST API) porque la nueva key `sb_secret_...` no funciona con la Storage REST API:

```sql
INSERT INTO storage.buckets (id, name, public)
  VALUES ('fotos', 'fotos', true) ON CONFLICT (id) DO NOTHING;
```

Políticas RLS en `storage.objects`:
```sql
-- SELECT: objetos públicos accesibles sin auth
CREATE POLICY "fotos_select" ON storage.objects FOR SELECT USING (bucket_id = 'fotos');
-- INSERT: requiere usuario autenticado
CREATE POLICY "fotos_insert" ON storage.objects FOR INSERT TO authenticated WITH CHECK (bucket_id = 'fotos');
```

Paths usados: `recetas/{receta_id}.{ext}`, `carta/{item_id}.{ext}`.

## Demo pública — `demo_visitas` + `reset_demo_restaurante()` (Q2, jul 2026)

`demo_visitas`: tabla nueva, solo `id, created_at` — contador de clicks en "Ver demo", sin `restaurante_id` ni PII. RLS: `INSERT` abierto a `anon, authenticated` (`WITH CHECK (true)`).

`reset_demo_restaurante()`: función plpgsql (`supabase/migrations/20260706_demo_reset_function.sql`) que borra todo lo que haya en el restaurante demo (`00000000-…-000002`) y lo re-clona fresco desde El Rescoldo real (`00000000-…-000001`) — 59 tablas (41 con `restaurante_id` + 18 hijas), ids regenerados y FKs remapeadas con tablas temp `old_id→new_id`. Invocada por el cron nocturno `GET /api/cron/reset-demo` (`CRON_SECRET`). **No clona** `restaurantes`/`user_restaurantes`/`equipo_miembros`/`rol_permisos` (resueltos una sola vez por `scripts/crear-cuenta-demo.mjs`) ni `perfiles`/`turnos` (ver comentario al inicio de la migración: `turnos` tiene `UNIQUE(miembro_id,fecha)` no scopeada por restaurante — forzar un solo miembro demo colisionaría). Si se agrega una tabla nueva con `restaurante_id` al schema, hay que sumarla a esta función o quedará vacía en la demo tras el próximo reset.

## Cómo verificar columnas reales

```bash
curl -X POST https://api.supabase.com/v1/projects/clipcxcbtlibswfzsgzk/database/query \
  -H "Authorization: Bearer $SUPABASE_MANAGEMENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query":"SELECT column_name,data_type FROM information_schema.columns WHERE table_name='\''nombre_tabla'\'' ORDER BY ordinal_position"}'
```

O usar la skill `/supabase-check nombre_tabla`.
