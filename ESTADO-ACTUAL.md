# KitchenOS — Estado Actual del Proyecto

**URL Producción:** https://kos-app-one.vercel.app
**Credenciales test:** admin@elrescoldo.com / kitchenos2026
**Supabase:** https://clipcxcbtlibswfzsgzk.supabase.co
**Restaurante:** El Rescoldo (Córdoba, Argentina)

Este archivo es una foto del presente (qué existe, qué falta). El detalle histórico sesión-por-sesión vive en `HISTORIAL.md`.

---

## 1. Módulos y Estado

| # | Módulo | Ruta | Estado | Resumen |
|---|--------|------|--------|---------|
| 1 | **Dashboard** | `/` | Funcional | Bloque **«Ahora»** primero (lee momento del día — apertura/control de carta/servicio/fuera de turno — con progreso y un único CTA al paso que sigue). Header con perfil real, contador real de 86 activos, ingresos + cuentas por pagar colapsados en un fold plegable, `ModulosGrid` ordenado por frecuencia de uso (6 más usados + "Ver todos"). |
| 2 | **Tareas** | `/tareas` | Funcional (redirige a OPS salvo emprendimiento) | CRUD, prioridades, categorías, plaza, checklist por tarea. En perfil "emprendimiento" es pantalla propia agrupada por área libre (Kanban). |
| 3 | **Recetario** | `/recetario` | Funcional | Lista con food cost, detalle, CRUD ingredientes (con etapas/grupo), importación IA (single+multi), foto, peso total/escurrido, costeo en tiempo real, unidades smart, auto-link a stock, "Convertir a plato" + panel OPS integrado. El alta carga **N etapas** en bloques (nombre editable, sus ingredientes y su propio "agregar" en cada uno) — una receta de una sola etapa se ve igual que siempre. Foto y escalado están en primer plano: la foto se carga en el alta (no solo en la edición) y el escalado tiene multiplicadores de un tap (½/×2/×3) además del número exacto. Tab Platos = ficha técnica derivada de la Carta: componentes con su gramaje real por porción (toma `peso_porcion` del mise cuando hay recipiente, editable inline) y total del plato. Crear abierto a todos; importar solo admin. Todo lo que muestra plata (food cost, costo/porción, precio sugerido, costo/kg, exportar XLSX, Salud del recetario) responde al permiso `ver_costos`, configurable por puesto — antes la ficha de receta lo mostraba sin ningún gate. |
| 4 | **Stock** | `/stock` | Funcional | Productos con umbral de alerta bajo/alto (ok/bajo — "crítico" ya no se distingue en pantalla, el campo sigue en DB en 0 — y "alto" cuando supera el `stock_maximo` opcional, techo de compra pensado para perecederos), CRUD con eliminar directo desde la fila, categorías, sectores/estantes físicos, producción interna vinculada a receta, "fuera de uso", modo rápido de conteo, import de planilla, carrito de compras, comparador de precios entre proveedores, sync de precio con facturas. `merma_esperada_pct` por producto (precarga con `scripts/precargar-mermas.mjs`, tolerancia para la futura detección de fuga, no entra al costeo) y `nota_recepcion` (estándar de recepción) en un desplegable secundario del modal. Stock de seguridad (mínimo × 1.25) se calcula al vuelo, no es columna. Tabla ordenable por nombre/valor/nivel de stock; edición inline de stock y mínimo con teclado nativo en mobile; modal de alta/edición centrado con blur en desktop. |
| 5 | **Pedidos** | `/pedidos` | Funcional | CRUD, ítems con precios, estados, productos frecuentes, WhatsApp/PDF, recepción parcial (genera una incidencia de proveedor sola si llega menos de lo pedido), rango de entrega + banner de ingresos. |
| 6 | **Proveedores** | `/proveedores` | Funcional | CRUD, CUIT, teléfono, rubro, días y horario de entrega, historial de facturas, auto-creación desde facturas con IA. Incidencias por proveedor (faltante/calidad/fuera de horario/precio/devolución): la `faltante` se auto-crea al recibir un pedido incompleto, el resto se carga a mano; resumen de los últimos 90 días por proveedor en hechos, sin score compuesto. |
| 7 | **Compras** | `/facturas` | Funcional | Título real en pantalla es "Compras" (5 tabs: Gastos, Recepción, Cat. de Gastos, Listas, Proveedores). Carga con ítems, OCR con IA, detección de variación de precio, cuentas por pagar (con vencimientos), reconciliación factura↔pedido, filtro de privacidad (gastos no-mercadería). |
| 8 | **Carta** | `/carta` | Funcional | Ítems vinculados a recetas, food cost, 86, categorías dinámicas, tags dietarios, panel OPS unificado (plaza→sección→recipiente→cantidad), importación IA, foto, carta pública QR sin login (`/carta/[slug]`). Precio, food cost, margen, cuadrante de ingeniería y la pantalla de Rentabilidad entera responden al permiso `ver_costos` (configurable por puesto), no a `isAdmin`. Editor unificado Plato/Menú/Evento (`ComposicionEditor`): costeo y switch de modo consistentes, buscador que no se cierra entre altas + gramaje inline con cadena Enter→sigue buscando, vistazo rápido de ingredientes en resultados, crear receta al vuelo con foto/texto IA (ingredientes editables, vinculables a stock existente o creables ahí mismo, procedimiento editable). Cada ítem de Menú o Evento tiene selector de prioridad SP/P/REF/Check y vigencia (desde/hasta). Desde `MenusView` → **"Activar en el mise"** (solo menú fijo — un evento se activa por fecha directo a Producción, no tiene este botón, ago 2026) sincroniza sus preparaciones con plaza+sección como `checklist_items` propios (idempotente, `lib/ops/menuMise.ts`), visibles en la apertura/cierre del Mise solo mientras esté vigente. Un evento igual puede terminar con presencia en el mise (heredada de antes de esa convención, o porque editarlo con `checklist_items` ya cargados lo vuelve a sincronizar — intencional, ver `DECISIONES.md` §21) — siempre en la plaza dedicada, nunca repartido. **Plaza de control** opcional (`menus.plaza_control`): una sola plaza controla el menú entero en vez de repartirlo por estación individual, con `OpsPanel` en modo `forcedPlaza` — default sugerido **`'menu'`**, la plaza dedicada del mise (ámbar, no física; nunca `'general'`, que duplicaba cada preparación en toda plaza real). **Rentabilidad** (dentro de Carta, 4 tabs): Lista (food cost/margen por plato), Ingeniería de menú (matriz Estrellas/Caballos/Puzzles/Perros — ver fila `~~23~~`), Reprecio (food cost objetivo → sugerencia masiva, con guardarraíl para FC > 200% sospechoso de error de unidades), Salud (sin receta vinculada, margen negativo). En desktop, cada tarjeta de la lista de platos es una carta con flip (`FlipCard`): dorso con precio/margen, tags, disponible y recetas de un vistazo, "Editar completo" como único salto a la pantalla de edición (recetas/packaging siguen viviendo solo ahí). Mobile sigue siendo tap directo a esa pantalla, sin flip. |
| 9 | **Checklist / Mise en Place** | `/checklist` | Funcional (vive embebido en OPS) | Mise por plaza, prioridades SP/P/REF/OK, rutinas con frecuencia, drag entre secciones, sub-secciones (1 nivel), plaza General, auditoría con scoring/foto obligatoria/condicionales. Un ítem puede venir de un menú (o, con presencia heredada, un evento) activado desde Carta (`checklist_items.menu_id`) — vive en su propia plaza dedicada **Menú** (ámbar, aparece en el selector solo si hay algo activado ahí) con chip del nombre del menú, y solo mientras esté dentro de su vigencia (`menuItemVisible()`), pensado para menús ejecutivos que se planifican por una o dos semanas. **Cualquier miembro entra a cualquier plaza** (la plaza asignada define dónde arranca, no qué mira; RLS nunca filtró por plaza). Header de 3 filas que se pliega a 1 al scrollear hacia abajo (`lib/ops/chromeBus.ts`, evento de window porque el que scrollea y el que se pliega son hermanos): el título es el selector de plaza+turno (ventana centrada con el avance de cada plaza), `?` y editar secciones en un menú de tres puntos. **Modo Control** deja cada ítem en una línea con tres decisiones y ningún número — tilde, badge de prioridad que cicla SP→P→REF, y `+` que despacha sin cantidad — y la fila se pinta según cómo quedó (blanca pendiente, ámbar despachada, verde tachada tildada), con los dos botones apagados en gris y deshabilitados una vez resuelta. En el cierre ese `+` **arma el pase de turno**: la tarea se crea para el turno siguiente real (`turnoSiguiente`, categoría `pase_turno`), así que un cierre en Modo Control hereda *qué falta y con qué prioridad* en vez de cuánto quedó — las cantidades se hablan en la cocina. Un ítem despachado cuenta como cerrado (no dispara el aviso rojo de "sin cierre") y el que entra lo ve en el aviso ámbar "Te dejaron en producción". La contra, asumida: ese cierre no deja el número de referencia, y la sugerencia de producción asume stock 0 para esos ítems. Tarjeta sin números redundantes: peso de porción junto al nombre, "hay ahora" en línea, déficit solo en el botón de producir (recalcula al tipear, auto-tilda si no falta nada), prioridad en el panel expandido. Grilla multi-columna en desktop con puntero real (el reordenar es un long-press táctil `clientY`-only). Botón "?" con guía de uso: hoja de lectura (cada control, qué hace y cómo repercute en el turno siguiente, incluido "la primera vez que cargás lo que hay") y recorrido guiado que la señala sobre la pantalla real. **El turno vigente lo decide la entrega, no el reloj**: al completar el cierre se entrega la plaza con un botón explícito (`cierres_turno`, por plaza, con autor y hora) y desde ese instante esa plaza está en el turno siguiente; el horario queda como red de contención para la plaza que se va sin cerrar. La entrega es un relevo, no solo un checklist (ago 2026, `EntregaPlazaSheet`, sin `window.confirm()` nativo): pide en 2-3 taps opcionales cómo salió el turno (bien/regular/complicado) y qué debe saber el que entra (`cierres_turno.percepcion`/`notas_servicio`). La apertura mide *ítems revisados*: despachar producción marca el ítem en ámbar "en producción" y suma al contador, y pasa a verde solo cuando la tarea se completa. Avisos persistentes al 100% (apertura descartable, cierre con la entrega). **Apertura y cierre se cuentan con los mismos atajos**: select-all en el campo precargado, Enter para saltar al siguiente sin contar (tarjeta centrada), auto-tilde cuando el dato completa el ítem (en apertura al llegar al target, en cierre al escribir el número — contar *es* la acción del cierre) y un CTA que convierte el faltante en tarea, "Producir X porc" hoy en apertura y "Producir mañana X" en cierre. Notas de plaza arriba de todo: el contexto que dejó el turno anterior, antes de empezar a tildar. Todo optimista y con sync instantáneo contra Producción en la misma pestaña + realtime entre dispositivos. El selector de plaza es una carta con flip (`FlipCard`, mismo componente que Organigrama): frente con progreso, dorso con la última entrega (hora + ítems) detrás de un ícono "i". Tildar un ítem y completar el 100% de una plaza tienen animación de confirmación (`tilde-pop`, `plaza-pulse` en verde). El equipo completo (apertura+cierre, todos los turnos activos) tiene su propia quest colectiva con celebración una vez por jornada, sin ranking de personas. |
| 10 | **Pase de Turno** | `/pase` | Funcional | Chat continuo entre turnos, prioridades, crear tarea desde mensaje, realtime, `@menciones` compartidas con la columna Importante de OPS. **Las notas de plaza son la misma tabla** (`pase_mensajes` con `plaza`, `useNotasPlaza`): lo que se escribe en la columna de una plaza en OPS o arriba del Mise se lee acá, y un mensaje de acá con `#parrilla` aparece en esa columna. Ventana de la nota: hoy + turno anterior. |
| 11 | **Limpieza y Mantenimiento (HACCP)** | `/haccp` | Funcional | Temperaturas (fecha calculada con `fechaEnTz`, no UTC — fix ago 2026), Vencimientos (color por días), Limpieza (con calendario + sync a OPS). Export PDF para Bromatología. |
| 12 | **Reportes / CMV** | `/reportes` | Funcional | 11 tabs: Resumen, Ventas, CMV, Rendimiento, Fuga, Food Cost, Compras, Precios, Producción, Caja, Auditoría. El tab Presupuesto se mudó a su propio módulo (`/presupuesto`, fila 33) ago 2026. Fuga compara consumo teórico (ventas × ficha técnica) contra compras del período por producto, con tolerancia de merma esperada y lista aparte de platos sin receta vinculada (`lib/reportes/fuga.ts`). Export Excel contextual por tab. Ventas → tab Meseros con ticket promedio y % de comandas con postre/café por mesero. **Reportes → Personal** (`/reportes/personal`, admin-only, fuera de las 11 tabs): una fila por persona con producción (turnos, tareas, score) **y** venta real fusionadas — ticket/postre/café junto a la distancia al objetivo del puesto (`puestos.objetivos`, override en `equipo_miembros.objetivos`), en texto neutro y sin ranking de ventas (`lib/reportes/ventasPorPersona.ts`). |
| 13 | **Calendario** | `/calendario` | Funcional | Grilla mensual estilo Google Calendar (celdas altas, píldoras de evento por color, botón Hoy, crear con un clic) + vista semanal. Notas por día como ítems individuales, cada uno enviable a Producción eligiendo plaza (crea la tarea real). "Planificar evento" (renombrado ago 2026, antes "Planificar menú"): elige un evento del catálogo + rango de fechas y activa la producción de todos esos días de una (comparte lógica con "Cargar menú" de Planificación vía `lib/menus/activarMenu.ts`) — solo eventos, un menú fijo se activa por vigencia en el mise, no por fecha. Refleja automáticamente entregas de pedidos y menús activados (tareas.menu_id) como eventos de solo lectura. "Nuevo evento" es modal centrado en desktop, full-screen en mobile. Plan de expansión (rutinas recurrentes, más reflejos, Coach con contexto completo) en `CALENDARIO-PLAN.md`. |
| 14 | **Turnos / Equipo** | `/turnos` | Funcional | Tabs Equipo/Turnos/Puestos. Sistema de puestos con nivel+plaza_default+módulos (checklist cubre los 27 módulos asignables, sincronizado con el catálogo real), overrides individuales por persona, fichaje real (clock-in/out → costo laboral). Invitación por email asigna puesto directo desde el modal (opcional). Cada puesto define además si **ve costos y food cost** (`puestos.ver_costos`, bloque propio y destacado en el editor — no un módulo más de la grilla), con override por persona en su ficha que cicla hereda/sí/no. Default `false` en todo salvo los puestos de nivel admin. Cada puesto puede llevar objetivos de venta opcionales (% comandas con postre/café, ticket promedio) — se ven fusionados con la venta real en Reportes → Personal. |
| 15 | **Producción / Planificación** | `/produccion` | Funcional (vive embebido en OPS) | Planilla del día, calendario mensual con multi-select (puntos verdes = menú, naranjas = evento), "Sugerir producción" con IA (motor de reglas + narración, nunca cambia números). Multi-menú por día separado visualmente: un bloque por menú activo (badge Menú/Evento, nombre, avance, vaciar solo ese) y filtro Todo/Menú/Evento con conteos, mismo criterio que el toggle de Producción. El picker de menú tiene **una sola puerta de activación por tipo** (ago 2026): un menú fijo solo se activa por vigencia en el **mise** (checklist con apertura/cierre, mismo estado vigente/futuro/vencido que Carta → Menús, `estadoMiseMenu()` compartido); un evento solo se activa por fecha/rango elegido, directo a Producción (`activarMenuParaFechas`) — antes convivían las dos rutas para cualquier tipo y podían duplicar tareas del mismo menú. El día/rango del picker (Calendario incluido, "Planificar evento") solo se muestra si el catálogo tiene algún evento. |
| 16 | **Merma** | `/merma` | Funcional | Bottom sheet + módulo propio, 8 motivos, turno, plaza, costo estimado (descuenta `stock_actual`). |
| 17 | **Configuración** | `/configuracion` | Funcional | Tabs restaurante/plazas/rutinas/permisos por rol. Carta pública (toggle+slug+QR), impresión y vencimientos HACCP configurables por establecimiento. |
| 18 | **Auth** | `/login`, `/register` | Funcional | Login/registro con onboarding, reset password, proxy.ts protege rutas. Al aceptar una invitación se cierra el vínculo `equipo_miembros.auth_user_id` vía `/api/invitar/vincular` (admin client, idempotente) — sin eso el invitado quedaba sin puesto y sin poder entrar. `lib/auth/context.tsx` lo reintenta como auto-reparación cuando un usuario con restaurante aparece sin ficha de equipo. Al primer ingreso ve una carta de bienvenida con su puesto, su plaza y los módulos que tiene habilitados. Botón "Ver demo" (clon lectura-escritura con reset nocturno). |
| 19 | **Perfil** | `/perfil` | Funcional | Avatar, datos, cambiar contraseña, cerrar sesión, "Ver de nuevo los recorridos" (limpia `equipo_miembros.tours_vistos`). |
| 20 | **Kitchen Coach (IA)** | API `/api/coach` + FAB | Funcional | Chat con FAB draggable, tours guiados por pantalla (21/21 con cobertura — Presupuesto sumado ago 2026) que **arrancan solos la primera vez** que la persona entra a cada una (`useTourAutomatico`, gateados por permiso de módulo y con el "ya lo vi" en DB), datos reales server-side (M1), tool use agéntico (crear tarea, marcar 86, registrar merma, sugerir producción — `crear_evento` todavía no setea plaza/prioridad/vigencia). Criterio de timing y tono (ago 2026): no sugiere cambios de receta/ideas durante servicio activo, y señala desvíos (food cost, faltantes) como dato a corregir, no como falla personal. Conoce menú/evento en el mise (vigencia, plaza de control, distinción con "Activar" de Planificación) y puede highlightear el botón Menús de Carta. **Gate de costos server-side**: sin `ver_costos` el modelo ni ve las tools de plata (`COACH_COST_TOOLS`: último precio, gasto del período, ventas, deudores) y el snapshot del prompt le llega sin food cost ni costo de merma — esconderlo en la UI no serviría, la respuesta se arma del lado del servidor. |
| 21 | **Modo Servicio / Salón** | `/salon` | Funcional | Mapa de mesas con forma/tamaño/rotación reales (editor canvas con zoom/pan), comandas con modificadores, cobro, arqueo de caja, Kitchen Coach propio. Sin conexión: bloqueado + banner. |
| 22 | **Ventas** | `/ventas` | Funcional | Importación desde Excel/CSV y texto libre con IA, revisión editable, KPIs, ranking de platos, food cost teórico. |
| 24 | **OPS — Workspace diario** | `/operaciones` | Funcional | Puerta de entrada única a Producción/Mise/Planificación/**Turno**. Producción: Carta y "Todo" agrupan por PLAZA (quién lo ejecuta), Menú/Evento por paso; prioridad ordena y colorea dentro de cada columna (SP/P arriba, REF/Check plegados salvo que no haya nada arriba, para no dejar columnas llenas viéndose vacías), drag&drop de columnas persistido. Densidad cómoda/compacta (fila de una línea) por dispositivo, botón junto al toggle. Cada columna de plaza tiene **notas de plaza** (arriba de las tareas, contador en el header si está plegada) y **pliega lo terminado antes de la entrega** detrás de "N listas antes de las HH:MM" — el turno que entra abre en lo suyo y no sobre lo que hizo el que salió; lo que se tilda después de entregar queda a la vista y a las 05:00 rueda todo con la jornada. El corte es por hora (`cierres_turno.cerrado_at` vs `tareas.completed_at`), no por identidad de turno, y solo aplica a Carta: Menú/Evento se agrupan por paso, no por plaza. La plaza **General** (no física, de control) se distingue en azul del resto. **Menú** es otra plaza no física — ámbar, plaza dedicada del mise para un menú fijo activado (antes usaba `'general'` y eso duplicaba cada preparación en toda plaza real de cocina a la vez, fix ago 2026, ver `hooks.md`). Una tarea despachada desde el mise para un ítem de menú va directo a la banda Menú con su paso real (Apetizer/Proteína/...), nunca aparece como columna de Carta ni duplica la columna que ya creó el plan; guarda además contra el doble despacho (dos cocineros o un doble-tap creando la misma tarea dos veces). **Tab Turno** (ago 2026): la rutina de apertura y cierre de la cocina entera — el papel que Bros tenia colgado, ahora tildable y editable. Dos fases (Apertura/Cierre) x turno de servicio, con la hora POR TURNO en el mismo item ("bacha en cero" es 11:00 en el almuerzo y 19:00 en la cena: se edita una vez, vale para los dos). Items exclusivos de un turno ("cenizas" solo de noche) y de un dia ("campana" solo jueves). Las zonas de limpieza del cierre piden **responsable** del equipo ademas del tilde — son los items que en el papel terminaban en dos puntos. Guarda la hora real de cada tilde contra la pautada, y avisa en ambar cuando el proximo paso con hora ya paso. Boton "imprimir" en el header exporta un PDF de una carilla A4 (apertura arriba, cierre abajo, casillero para tildar a mano) para colgar en la pared (`exportRutinaTurnoPDF` en `lib/exportPDF.ts`). Tablas propias (`rutina_turno_items`/`rutina_turno_registros`), **no confundir con `checklist_rutina`**, que es limpieza por plaza con frecuencia y auditoria. El par (jornada, turno) sale de `turnoVigente`, no de `hoyOperativo`+`turnoActivo` por separado: entre el corte de jornada y el primer turno esos dos devuelven un par que nunca existio. **Instructivo para la cocina**: `docs/ops-modo-control-una-hoja.html` — una A4 imprimible que explica el día en Modo Control con capturas reales de la app y cada botón recortado (se regenera con la skill `hoja-instructiva`). **Control de carta** (ago 2026, PLAN-4-CAPAS B7): banner que aparece solo en la ventana previa a que arranque un turno (`proximoTurnoEnVentana`), lleva a `/control-carta` — checklist de la carta agrupado por plaza (vía `plato_recetas.plaza`; `plato_plazas` está vacía en cuentas reales, no sirve para esto) con tres estados por plato y ningún número: `ok` / `ajustar` (crea tarea de producción en la plaza real) / `no_sale` (marca el 86 automático en Carta, y lo levanta si se corrige después). Los 4 tabs (Producción/Mise/Planificación/Turno) también se navegan con swipe horizontal — scroll-snap nativo, no gesto calculado a mano (fix ago 2026: Planificación tenía su propio scroll interno que se comía el swipe viejo), y el tab Turno tiene sombra/botones con superficie propia en vez del borde de 1px plano. En mobile, `BottomNav` suma un 6to ícono contextual (Carta en ventana de control de carta, Mise el resto del tiempo). **Modo pantalla completa** del board de Producción (ícono junto a los tabs): tapa el chrome de la app y pide fullscreen al navegador, con escala regulable 100-160% (`zoom`, que reflowea — `transform: scale` cortaría las columnas), salida por Escape o botón siempre visible, y el primer link a El Muro que existe en toda OPS. El board queda entero e interactivo: se siguen tildando las tareas desde ahí. Pensado para la tablet colgada en la cocina, por eso el estado se persiste. |
| 25 | **Mesa de Trabajo** | `/espacios` | BETA (solo desktop) | Tab Producción (board anidado espacio→plaza→sección→sub-sección→producción) + tab Stock (Kanban productos×sectores con drag&drop, multi-select, columnas colapsables) + tab Carta (board de platos con OPS por plaza). Plazas custom por restaurante (JSONB, sin tabla nueva). |
| 26 | **KDS (Kitchen Display)** | `/kds` | Funcional | Selector de estación, tarjetas por comanda con cronómetro por umbral, bump por ítem/comanda, labels en español, sin Coach (regla inamovible), offline con cola IndexedDB. Marcar 86 (agotado) y los botones del header van por sheet propio, sin diálogos nativos del navegador (ago 2026). |
| 27 | **Modo Emprendimiento** | perfil por restaurante | Funcional (piloto: VOGLIO Farina) | `restaurantes.configuracion.perfil='emprendimiento'` oculta módulos de servicio de restaurante incluso siendo admin; `/tareas` propia; Mesa de Trabajo/Stock sin auto-seed de plazas de restaurante. |
| 28 | **Muro** | `/muro` | Funcional (sin verificar en tablet real) | Tablet única para toda la cocina, solo admin/chef. Columnas por plaza sobre Producción (no el Mise): pendientes listados por prioridad, listos colapsan a "+N listas", plazas sin pendientes se encogen si hay más de 5. Tap cicla pendiente→en_curso→listo, toque largo marca duda (sube a franja de alertas con quién y hace cuánto), tap en el encabezado enfoca esa plaza a pantalla completa. Franja de entregas por plaza abajo. Notas de plaza en solo lectura bajo el encabezado de cada columna, sin poder plegarse (en una tablet que nadie toca, lo que se esconde queda escondido) — una plaza con nota no se encoge. Wake lock + refetch al volver de suspensión, rollover de jornada por timer. |
| 29 | **Clientes** | `/clientes` | Funcional | No estaba documentado acá (encontrado ago 2026). Dos tabs: Clientes y Cuentas Corrientes (fiado). Filtros por cantidad de compras, última compra, grupo. Se vincula al cobrar en Salón. |
| 30 | **Bitácora** | `/bitacora` | Funcional (F1) | Hoja de ruta / reuniones de equipo — reemplaza el Google Docs de lluvia de ideas. Lista de entradas (reunión/nota/lista/idea) con buscador y filtro + documento tipo-doc: Enter parte la línea y crea la siguiente, Tab indenta, Backspace al inicio fusiona con la anterior, checkbox por ítem, pegar texto multilíneas desde Docs lo parte en ítems. Participantes desde el día 1 (chips contra el equipo real). Creación de ítems optimista de punta a punta (id generado en el cliente); la entrada abierta persiste en sessionStorage y el borrador sin confirmar se guarda solo al cambiar de pestaña/app o cerrar. Solo admin/chef. Falta F2 (ítem→tarea de OPS, arrastre de pendientes a la próxima reunión) y F3 (plantillas, ítem→pase, export). |
| 31 | **Organigrama** | `/organigrama` | Funcional (Fase 1-3) | Cartas de puesto con flip (Plantel), catálogo fijo de 12 áreas siempre visibles con su explicación aunque estén inactivas (Estructura — árbol de puestos por área, reasignar jerarquía), Vista Cobertura (área activa × Definir/Preparar/Ejecutar/Controlar, alerta roja si falta responsable salvo en Ejecutar, que por defecto es "todo el equipo"). Multi-responsable: un área o capa puede tener más de una persona a cargo. Asistente de 3 preguntas activa áreas según tamaño/tipo de negocio y te deja como responsable de Dirección. Export PDF (organigrama completo por área + una hoja por puesto, tipo manual de puesto). Kitchen Coach integrado (tour de 9 pasos, sugerencias, highlight). Desde el dorso de cada carta, "Editar accesos" (admin) deep-linkea a la ficha de esa persona en Turnos → Equipo con el editor de módulos ya abierto. Tablas `areas`/`area_capas`, columnas nuevas en `puestos` (`area_key`, `reporta_a_puesto_id`, `orden`). |
| ~~23~~ | ~~OPS — Ingeniería de Menú~~ | ~~`/ingenieria-menu`~~ | **Movido, no eliminado** | La pantalla vieja se sacó, pero la función sigue viva y más completa dentro de Carta → Rentabilidad → Ingeniería (matriz Estrellas/Caballos/Puzzles/Perros + Reprecio + Salud, ver fila 8). |
| 32 | **Reservas** | `/reservas` | Funcional, aislada (ago 2026, PLAN-4-CAPAS B8) | Vista Día (lista por hora con pax/teléfono/estado) y Semana (carga por día, cubiertos vivos). Estados pendiente/confirmada/sentada/no_show/cancelada, origen teléfono/WhatsApp/web/walk-in. Todavía sin enganches a OPS/Salón/Calendario/Dashboard ni a la sugerencia de producción/compra — eso es B9/B10, pendientes de que el track de validación con Bros/Rescoldo confirme que vale la pena. Solo admin/chef; oculto en perfil emprendimiento. |
| 33 | **Presupuesto** | `/presupuesto` | Funcional (ago 2026, PLAN-PRESUPUESTO-CMV-2026-08) | El estándar declarado contra el real, gatead por `usePermisos().verCostos`. Tab **CMV por sector**: hero con CMV del mes vs. objetivo 30% (desvío en puntos + $, proyección a fin de mes, saldo), tabla de sectores (objetivo expresado sobre ventas — así el desvío de cada fila suma exacto el desvío total — vs. real, presupuesto editable por sector con "Usar sugerido" que siembra del mix histórico de 3 meses), ritmo de compra semanal (bloques de días 1-7/8-14/15-21/22-28/29-fin, gasto contra presupuesto semanal — NO contra ventas, las compras entran a saltos), y desperdicio (Merma) + arreglos y mejoras (categorías con `es_mejora`, no confundir con devoluciones de proveedor). Tab **Familias**: el viejo tab Presupuesto de Reportes, movido tal cual (4 familias de gasto, EBITDA, "Usar estructura estándar"). Tablas `presupuesto_mes`/`presupuesto_sector` + flags `categorias_gasto.cuenta_en_cmv`/`es_mejora` (ver `columnas.md`). Selector de mes propio (no el `Periodo` de Reportes — acá siempre mensual). |

**Resumen:** 31 módulos funcionales (incluye salón, KDS, muro, modo emprendimiento piloteado, clientes, bitácora, reservas), 0 parciales, 0 críticos pendientes.

---

## 2. Tablas de Supabase (86 total — 78 regenerado 11/08 + `bitacora_entradas`/`bitacora_items` 13/08 + `proveedor_incidencias` 18/08 + `rutina_turno_items`/`rutina_turno_registros` 19/08 + `reservas` 22/08 + `presupuesto_mes`/`presupuesto_sector` 25/08; verificado contra `information_schema` el 19/08)

Ver `ARQUITECTURA.md` §5 para el desglose completo por dominio (20 grupos, desactualizado en el conteo — pendiente próxima regeneración) y §Supabase para relaciones clave. Columna por columna → `.claude/docs/columnas.md`.

**Total: 84 tablas** con RLS habilitado. Aislamiento multi-tenant real vía `mi_restaurante_id()` (ver `.claude/docs/rls.md`).

---

## 3. Bugs / Deuda Técnica Conocida

### Deuda técnica abierta
| # | Severidad | Descripción | Archivo |
|---|-----------|-------------|---------|
| 1 | Media | `useCallback` deps vacías en algunos hooks que usan `RESTAURANTE_ID` — riesgo de stale closure si cambia restaurante. | `lib/hooks/*.ts` |
| 2 | Media | Tipos desactualizados: campos legacy en `types/index.ts` que no matchean el schema DB actual en algunos casos puntuales. | `types/index.ts` |
| 3 | Baja | Modo Servicio (`ModoServicio.tsx`) no conectado — probablemente se descarta, ver `DECISIONES.md`. | `components/dashboard/ModoServicio.tsx` |
| 4 | Info | Scripts de migración con token de Supabase hardcodeado en algunos `.mjs`. | `scripts/*.mjs` |
| 5 | Info | Vitest: 173 tests en 11 archivos (máquina de estados, turnos/pase de turno, bus del mise, target/déficit, sincronización menú↔mise, resolución de permisos y sync tilde→tarea). Playwright: `e2e/salon-kds.spec.ts` (requiere `npx playwright install chromium` + dev server). Testing Library para hooks pendiente. | — |
| 6 | Media | `npm run lint` **corre** desde 25/08 (era un `node_modules/tsconfig-paths` incompleto, se reinstaló) y deja a la vista ~10.000 problemas preexistentes: 1021 errores, casi todos `no-explicit-any` en `scripts/`. No está wireado a CI y no puede estarlo hasta podar eso. Criterio mientras tanto: comparar el conteo de errores **por archivo tocado** antes y después del cambio. | `eslint.config.mjs`, `scripts/*` |

### Resueltos (histórico)
RLS multi-tenant real (44 políticas UPDATE con `WITH CHECK`), login con hard-navigation, matching Facturas→Stock, descuento de stock por merma, `USUARIO_MOCK` hardcoded — todos con causa raíz y fix documentados en `HISTORIAL.md` y en `.claude/docs/hooks.md`.

---

## 4. Arquitectura — resumen

```
Next.js 16.2.0 (App Router, Turbopack) + React 19.2.4
Supabase (auth + DB + realtime) + Vercel deploy
Tailwind CSS v4 + CSS vars + Material Symbols Outlined
jsPDF + jspdf-autotable | xlsx | Anthropic API (Haiku + Sonnet 4.6)
Auth: proxy.ts (NO middleware.ts — breaking change Next 16)
```

**Shell mobile:** `#shell` (en `app/globals.css`) es full-width en celular; la columna centrada de `max-width:420px` arranca recién en 600px (`sw600dp`) y se suelta en ≥1024px, donde manda `DesktopShell`. El cap no puede volver a un ancho de celular: hay Android que reporta 480-540 CSS px. Detalle en `.claude/docs/ui.md` § "Ancho del `#shell`".

**Desktop — paleta de comandos:** `Ctrl/Cmd+K` (`components/desktop/CommandPalette.tsx`, montada en `DesktopShell`) — ir a cualquier módulo permitido, registrar merma (abre el `MermaBottomSheet` real), crear tarea al vuelo. Listada en `ShortcutsHelp` junto al resto de los atajos de desktop.

```
app/
  (app)/          ← rutas protegidas (dashboard + módulos)
  (auth)/         ← login + register (públicas)
  (servicio)/     ← salon/page + kds/page + muro/page (fondo oscuro fijo en KDS/Muro, tema app en Salón)
  (publico)/      ← carta/[slug] (vidriera QR sin login)
  api/            ← coach, facturas, listas-precios, recetas/import, recetas/save, ingest/escpos, ...
lib/
  auth/           ← AuthProvider context + RouteGuard
  hooks/          ← hooks por módulo (useRecetas, useStock, useTareas, useComandas, useMesas, ...)
  supabase/       ← client (browser), server (SSR), admin (service role)
  fiscal/         ← interfaz ProveedorFiscal + stub
  comanda/        ← stateMachine.ts + tests (máquina de estados comanda/ítem)
  offline/        ← bumpQueue.ts (IndexedDB cola bumps), useOnlineStatus.ts
  ops/            ← mise.ts (helper compartido de asignación OPS/mise)
  constants.ts    ← Roles, módulos, nav
components/
  ui/             ← componentes canónicos D0 (SegmentedTabs, FilterChips, EmptyState, HeaderAction, Avatar, Num)
  dashboard/      ← Header, MiPlaza, ModoServicio, ModulosGrid, PasePreview, StockCritico, WelcomeDashboard
  shell/          ← BottomNav, SidebarNav, Header, MoreMenu, PageHeader, RouteGuard
  ops/            ← OpsPanel (fuente única del panel plaza→sección→recipiente→cantidad), ProduccionBoard, CrearTareaSheet
  coach/          ← KitchenCoachFAB
  merma/          ← MermaBottomSheet
types/
  index.ts        ← Todos los tipos (Receta, Tarea, Producto, …)
proxy.ts          ← Auth middleware (Next 16)
```
